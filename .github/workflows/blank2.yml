name: Test
on:
  workflow_dispatch:
jobs:
  AutoSyncProcess:
    runs-on: ubuntu-latest
    steps:
    - name: Start
      run: |
          echo -e "3\n" | bash <(curl -Lso- https://git.io/superspeed)
