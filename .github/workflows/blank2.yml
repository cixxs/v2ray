name: Test
on:
  workflow_dispatch:
jobs:
  AutoSyncProcess:
    runs-on: ubuntu-latest
    steps:
    - name: Start
      run: |
          wget -qO- --no-check-certificate https://raw.githubusercontent.com/cixxs/v2ray/rm/speedtest.sh | sudo bash 
